// ============================================================
// Hand-authored / generated puzzle library.
//
// Emptied deliberately: the app's puzzles now come from
// puzzles-ctc.js, which is built by tools/import-ctc from real
// puzzles published by their setters and verified to have exactly
// one solution under this app's rules.
//
// This file stays because it creates window.PuzzleLibrary (and the
// generators under tools/generate-* still write entries in this
// format if we ever want to seed puzzles here again).
// ============================================================

(() => {
  window.PuzzleLibrary = [];
})();
